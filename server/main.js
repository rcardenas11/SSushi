import { Meteor } from 'meteor/meteor';

import "../imports/api/reservas.js";
import "../imports/api/menu.js";
import { Accounts } from 'meteor/accounts-base';

Meteor.startup(() => {
  // code to run on server at startup
});
